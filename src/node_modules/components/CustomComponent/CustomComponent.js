import React from 'react';
import s from './CustomComponent.css';

class CustomComponent extends React.Component {
  render() {
    return (
      <div className = {s.root} >
        {this.props.children}
      </div>
    )
  }
}

export default CustomComponent;
